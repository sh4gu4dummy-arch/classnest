# QA Supervisor (meta-QA / top manager)

Search keywords: **QA supervisor**, **QAsup**, **meta-QA**, **top manager**, **feedback-on-feedback**, **Ash-direct**, **action bar**, **commit-per-id**.

## Status now (2026-09-19)

- **QAsupervisor is sleeping** (not deleted). Ash may wake or replace a QAsupervisor later.
- **Ash is the hard gate and primary contact** for ClassNest-Bot1 (QA).
- **Ash-direct default:** send Ash results, asks, and drafts as he prefers. Minimize mid-task spam unless he asks for progress.
- If a QAsupervisor returns: resume **draft → Round-1 markup → counter → align → ship only on Ash (or Ash→sup) explicit go**. Markup ≠ ship. Ash word still wins. No Ash spam from QA while a supervisor is live unless Ash asks in 1:1. FYI-to-Builder = hold until the plan is on the git desk.

## Protocol snapshot

1. **QA draft** (prompts / autopsy / SOP) → **QAsup Round 1** (when a supervisor is awake) → counter → align → **ship only on explicit go** (Ash, or Ash via supervisor).
2. Stern but flexible: **quality + creativity over rigid rules**. If a rule hurts production or creative outcome, **flag Ash** (or QAsup when awake) — do not blind-enforce.
3. When a loop finishes: status + **whose court the ball is in**.
4. **Approval-card / push block:** ping Ash **immediately** (ClassNest learned silence on a card wastes the night).
5. **Git is source of truth.** Fill AUDIT tables with **pixel evidence**. Do not rubber-stamp. Twin HARD only with **two distinct heads/bodies** (not reflections / one long body).

## Shared lessons from Ash (via this QAsup)

- **Homes 21–25:** not enough movement/action; keep scenery quality; more exciting motion + matching diegetic sound next rounds. Do not remake 21–25 unless named.
- **Trial-first** for new methods (anti-twin, action bar, extend chains) before burning five-packs when inventing process — unless Ash names a batch-together go.
- **Twins** = two distinct heads/bodies — don’t HARD FAIL reflections / looped long bodies as twins.
- **Commit-per-id** / one-by-one video track on shoots (41+ style).
- **Multi-still / lock frames** (or last-frame extend) for smoother I2V when plans benefit; avoid 15s-from-one-still twin factory.
- **Limb locks** on bipedal/humanized animals (31 Rookplate: wings became arms).
- **Deepen prompts** before batch go (33–35 style: catalog rewrite, virtue-action not souvenir-pickup).
- Catalog s3 with humanized limbs or real-language glyphs → **I2I must rewrite** before I2V.

## ClassNest-specific snapshot (under this supervisor)

- 21–25 Ash motion feedback → START / ultra-home-films ACTION BAR.
- 26–30 twin over-call HOLD → corrected autopsy (tableau/set/identity fails; no twin HARD).
- 31–35: limb lock; virtue-action; deepen 33–35; batch go.
- 36–40 keyframe A/B/C then lesson → 41–50 method v2 last-frame extend; commit-per-id.
- Action bar + anti-twin **prevention** (method, not longer negatives).
- Approval-card silence miss → **immediate Ash ping** rule.
- Progress (desk as of handoff): Homes through **55** in catalog; remaining **56–75**; next `start` audit = **56 Noxquill** (see START-HERE).

## Routing cheat sheet

| Who awake | QA contacts |
|---|---|
| QAsup sleeping (now) | **Ash-direct** |
| QAsup awake | Draft/markup to QAsup; Ash only for asks he made in 1:1 or approval blocks |
| Conflict | **Ash word wins** |
